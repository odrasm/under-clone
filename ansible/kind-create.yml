---
- name: kind-underground
  hosts: localhost
  connection: local
  become: True
  vars:
    ansible_python_interpreter: '{{ ansible_playbook_python }}'
    cluster_name: underground
    kind_create: true
  roles:
    - role: kind
